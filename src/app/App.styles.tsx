import { css } from '@emotion/css';

const Styles = {
  mainLayout: css({
    boxShadow: '0 0 48px 0 rgba(0, 0, 0, .2)',
    backgroundColor: '#fff',
    minHeight: '100vh',
    maxWidth: '500px',
    margin: '0 auto',
    position: 'relative',
    overflow: 'hidden'
  })
};

export { Styles };
