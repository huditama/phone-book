import { css } from '@emotion/css';

const Styles = {
  container: css({
    backgroundColor: '#E1E1E1',
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'row',
    padding: '5px 8px 5px 8px',
    borderRadius: '4px',
    height: '30px',
  }),
  input: css({
    outline: 'none',
    border: 'none',
    backgroundColor: 'transparent',
    width: '100%',
  }),
};

export { Styles };
